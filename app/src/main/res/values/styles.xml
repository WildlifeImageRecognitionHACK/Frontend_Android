<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
    </style>

    <style name="label_buttons">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textAlignment">center</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:padding">8dp</item>
        <item name="android:background">@drawable/rounded_button_bg_transparent</item>
        <item name="android:backgroundTint">@color/greyLight</item>
        <item name="android:fontFamily">@font/rubik_medium</item>
        <item name="android:textColor">@color/greyDark</item>
        <item name="android:clickable">true</item>
        <item name="android:onClick">categoryOnClick</item>
    </style>

</resources>
